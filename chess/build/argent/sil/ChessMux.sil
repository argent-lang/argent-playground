pragma silverscript ^0.1.0;

// Generated by argentc. Do not edit by hand.

contract ChessMux(
    byte[32] gen__init_chess_settle_template,
    byte[32] gen__init_player_template,
    byte[32] gen__init_chess_mux_template,
    byte[256] gen__init_chess_mux_routes,
    byte[32] init_white_player,
    byte[32] init_black_player,
    byte[64] init_board,
    int init_turn,
    int init_status,
    int init_move_timeout,
    byte[4] init_castle_rights,
    int init_en_passant_idx,
    int init_pending_src_idx,
    int init_pending_dst_idx,
    int init_pending_promo,
    int init_recent_castle,
    int init_draw_state
) {
    // :: shared constants
    int constant WHITE = 0;
    int constant BLACK = 1;
    int constant LIVE = 0;
    int constant WWIN = 1;
    int constant BWIN = 2;
    int constant DRAW = 3;
    int constant CLEAR = 0;
    int constant OFFER = 1;
    int constant CLAIM = 2;
    int constant SURRENDER = 3;
    int constant ACCEPT = 4;
    int constant CLAIMED = 1;
    int constant DEFENSE = 2;
    int constant NORMAL = 3;
    int constant WOFFER = 4;
    int constant BOFFER = 5;

    // :: state layouts
    struct LeagueState {
        // :: generated fields
        byte[32] gen__chess_mux_template;
        byte[32] gen__chess_settle_template;
        byte[32] gen__player_template;
        byte[32] gen__chess_mux_routes_digest;
        // :: user declared fields
        int base_rating;
        byte[32] admin;
    }
    struct PlayerState {
        // :: generated fields
        byte[32] gen__chess_mux_template;
        byte[32] gen__chess_settle_template;
        byte[32] gen__player_template;
        byte[32] gen__chess_mux_routes_digest;
        // :: user declared fields
        byte[32] owner;
        byte[32] player_id;
        int open_games;
        int rating;
        int games;
        int wins;
        int draws;
        int losses;
    }
    struct SettleState {
        // :: generated fields
        byte[32] gen__chess_mux_template;
        byte[32] gen__chess_settle_template;
        byte[32] gen__player_template;
        byte[32] gen__chess_mux_routes_digest;
        // :: user declared fields
        byte[32] white_player;
        byte[32] black_player;
        int status;
    }

    // :: shared helper functions
    function player_ref(byte[32] owner, byte[32] player_id) : byte[32] {
        return blake2b(owner + player_id);
    }
    function abs_int(int value) : int {
        int result = value;
        if (value < 0) {
            result = 0 - value;
        }
        return result;
    }
    function timeout_status(int turn, int draw_state) : int {
        int status = WWIN;
        if (turn == WHITE) {
            status = BWIN;
        }
        if (draw_state == CLAIMED) {
            status = DRAW;
        }
        return status;
    }

    // :: route templates
    byte[32] gen__chess_settle_template = gen__init_chess_settle_template;
    byte[32] gen__player_template = gen__init_player_template;
    byte[32] gen__chess_mux_template = gen__init_chess_mux_template;
    byte[256] gen__chess_mux_routes = gen__init_chess_mux_routes;

    // :: state fields: ChessMux
    byte[32] white_player = init_white_player;
    byte[32] black_player = init_black_player;
    byte[64] board = init_board;
    int turn = init_turn;
    int status = init_status;
    int move_timeout = init_move_timeout;
    byte[4] castle_rights = init_castle_rights;
    int en_passant_idx = init_en_passant_idx;
    int pending_src_idx = init_pending_src_idx;
    int pending_dst_idx = init_pending_dst_idx;
    int pending_promo = init_pending_promo;
    int recent_castle = init_recent_castle;
    int draw_state = init_draw_state;

    // :: entrypoints
    // :: leader entry (1:N)
    entrypoint function route(
        int target,
        int from_x,
        int from_y,
        int to_x,
        int to_y,
        int promo_piece,
        int termination_action,
        sig s,
        pubkey pk,
        byte[32] player_id,
        byte[] gen__target_prefix,
        byte[] gen__target_suffix
    ) {
        // :: auth outputs
        require(OpAuthOutputCount(this.activeInputIndex) == 1);
        int gen__next_output_idx = OpAuthOutputIdx(this.activeInputIndex, 0); // emits one Move

        require(status == LIVE);
        byte[32] player_ref = player_ref(blake2b(pk), player_id);
        if (turn == WHITE) {
            require(player_ref == white_player);
        } else {
            require(player_ref == black_player);
        }
        require(checkSig(s, pk));
        require(termination_action == CLEAR || termination_action == OFFER);
        int next_draw_state = draw_state;
        require(from_x >= 0 && from_x < 8 && from_y >= 0 && from_y < 8);
        require(to_x >= 0 && to_x < 8 && to_y >= 0 && to_y < 8);
        require(promo_piece == 0 || (target == 0 /*CHESS_PAWN*/ && promo_piece >= 2 && promo_piece <= 5));
        require(draw_state >= NORMAL || target <= 5 /*CHESS_KING*/);
        require(termination_action == CLEAR || (target <= 6 /*CHESS_CASTLE*/ && draw_state >= NORMAL));
        if (draw_state > NORMAL) {
            next_draw_state = NORMAL;
        }
        if (termination_action == OFFER) {
            next_draw_state = WOFFER + turn;
        }
        int next_recent_castle = CLEAR;
        if (target == 7 /*CHESS_CASTLE_CHALLENGE_PREP*/) {
            require(recent_castle != CLEAR);
            next_recent_castle = recent_castle;
        }
        State next_state = {
            // :: generated fields
            gen__chess_settle_template: gen__chess_settle_template,
            gen__player_template: gen__player_template,
            gen__chess_mux_template: gen__chess_mux_template,
            gen__chess_mux_routes: gen__chess_mux_routes,
            // :: user declared fields
            white_player: white_player,
            black_player: black_player,
            board: board,
            turn: turn,
            status: status,
            move_timeout: move_timeout,
            castle_rights: castle_rights,
            en_passant_idx: en_passant_idx,
            pending_src_idx: from_y * 8 + from_x,
            pending_dst_idx: to_y * 8 + to_x,
            pending_promo: promo_piece,
            recent_castle: next_recent_castle,
            draw_state: next_draw_state,
        };
        require(tx.outputs[gen__next_output_idx].value == tx.inputs[this.activeInputIndex].value);
        int gen__target_selector = target;
        require(gen__target_selector >= 0);
        require(gen__target_selector < 8);
        byte[32] gen__target_template = byte[32](
            gen__chess_mux_routes.slice(gen__target_selector * 32, gen__target_selector * 32 + 32)
        );
        // :: become target
        validateOutputStateWithTemplate(
            gen__next_output_idx,
            next_state,
            gen__target_prefix,
            gen__target_suffix,
            gen__target_template
        );
    }

    // :: leader entry (1:N)
    entrypoint function terminate(int termination_action, sig s, pubkey pk, byte[32] player_id) {
        // :: auth outputs
        require(OpAuthOutputCount(this.activeInputIndex) == 1);
        int gen__next_output_idx = OpAuthOutputIdx(this.activeInputIndex, 0); // emits one ChessMux

        require(status == LIVE);
        byte[32] player_ref = player_ref(blake2b(pk), player_id);
        if (turn == WHITE) {
            require(player_ref == white_player);
        } else {
            require(player_ref == black_player);
        }
        require(checkSig(s, pk));
        int next_status = status;
        int next_turn = turn;
        int next_draw_state = draw_state;
        if (termination_action == CLAIM) {
            require(draw_state == NORMAL);
            require(recent_castle == CLEAR);
            next_turn = 1 - turn;
            next_draw_state = CLAIMED;
        } else if (termination_action == SURRENDER) {
            next_status = BWIN - turn;
            next_draw_state = NORMAL;
        } else {
            require(termination_action == ACCEPT);
            require(draw_state + turn == BOFFER);
            next_status = DRAW;
            next_draw_state = NORMAL;
        }
        State next_state = {
            // :: generated fields
            gen__chess_settle_template: gen__chess_settle_template,
            gen__player_template: gen__player_template,
            gen__chess_mux_template: gen__chess_mux_template,
            gen__chess_mux_routes: gen__chess_mux_routes,
            // :: user declared fields
            white_player: white_player,
            black_player: black_player,
            board: board,
            turn: next_turn,
            status: next_status,
            move_timeout: move_timeout,
            castle_rights: castle_rights,
            en_passant_idx: -1,
            pending_src_idx: -1,
            pending_dst_idx: -1,
            pending_promo: CLEAR,
            recent_castle: CLEAR,
            draw_state: next_draw_state,
        };
        require(tx.outputs[gen__next_output_idx].value == tx.inputs[this.activeInputIndex].value);
        // :: become ChessMux
        validateOutputState(gen__next_output_idx, next_state);
    }

    // :: leader entry (1:N)
    entrypoint function timeout(
        sig s,
        pubkey pk,
        byte[32] player_id,
        byte[] gen__chess_settle_prefix,
        byte[] gen__chess_settle_suffix
    ) {
        // :: auth outputs
        require(OpAuthOutputCount(this.activeInputIndex) == 1);
        // :: emits one ChessSettle
        int gen__next_output_idx = OpAuthOutputIdx(this.activeInputIndex, 0);

        require(status == LIVE);
        byte[32] player_ref = player_ref(blake2b(pk), player_id);
        if (turn == WHITE) {
            require(player_ref == black_player);
        } else {
            require(player_ref == white_player);
        }
        require(checkSig(s, pk));
        require(this.age >= move_timeout);
        SettleState next_state = {
            // :: generated fields
            gen__chess_mux_template: gen__chess_mux_template,
            gen__chess_settle_template: gen__chess_settle_template,
            gen__player_template: gen__player_template,
            gen__chess_mux_routes_digest: blake2b(gen__chess_mux_routes),
            // :: user declared fields
            white_player: white_player,
            black_player: black_player,
            status: timeout_status(turn, draw_state),
        };
        require(tx.outputs[gen__next_output_idx].value == tx.inputs[this.activeInputIndex].value);
        // :: become ChessSettle
        validateOutputStateWithTemplate(
            gen__next_output_idx,
            next_state,
            gen__chess_settle_prefix,
            gen__chess_settle_suffix,
            gen__chess_settle_template
        );
    }

    // :: leader entry (1:N)
    entrypoint function settle(byte[] gen__chess_settle_prefix, byte[] gen__chess_settle_suffix) {
        // :: auth outputs
        require(OpAuthOutputCount(this.activeInputIndex) == 1);
        // :: emits one ChessSettle
        int gen__next_output_idx = OpAuthOutputIdx(this.activeInputIndex, 0);

        require(status != LIVE);
        SettleState next_state = {
            // :: generated fields
            gen__chess_mux_template: gen__chess_mux_template,
            gen__chess_settle_template: gen__chess_settle_template,
            gen__player_template: gen__player_template,
            gen__chess_mux_routes_digest: blake2b(gen__chess_mux_routes),
            // :: user declared fields
            white_player: white_player,
            black_player: black_player,
            status: status,
        };
        require(tx.outputs[gen__next_output_idx].value == tx.inputs[this.activeInputIndex].value);
        // :: become ChessSettle
        validateOutputStateWithTemplate(
            gen__next_output_idx,
            next_state,
            gen__chess_settle_prefix,
            gen__chess_settle_suffix,
            gen__chess_settle_template
        );
    }

}
